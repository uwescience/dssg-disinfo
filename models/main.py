import run_model
from run_model import run_model

if __name__ == "__main__":
    run_model('multiple')
    
    
#bidir_num_filters=64, dense_1_filters=10, vocab_size=10000, embedding_dim=300, maxlen=681, optimizer='adam'